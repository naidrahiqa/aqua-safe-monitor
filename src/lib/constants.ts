// ===================================================================
// Shared constants — single source of truth for hardcoded values.
// ===================================================================

/** Default map center (Jepara, Indonesia) */
export const DEFAULT_LAT = -6.5833;
export const DEFAULT_LNG = 110.6667;

/** Default map zoom level */
export const DEFAULT_ZOOM = 13;

/** Online/offline threshold in seconds */
export const ONLINE_THRESHOLD_SEC = 120;

/** Status update interval in ms */
export const STATUS_INTERVAL_MS = 5000;

/** Max items per page in data table */
export const TABLE_PAGE_SIZE = 10;

/** Max alerts shown in notification panel */
export const MAX_DANGER_ALERTS = 10;
export const MAX_WARNING_ALERTS = 5;

/** Minimum password length */
export const MIN_PASSWORD_LENGTH = 6;

/** Recent readings count for trend calculation */
export const TREND_READING_COUNT = 10;
